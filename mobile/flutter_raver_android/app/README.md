# Generated Flutter App Area

The Flutter project should be generated here after Flutter SDK is installed.

Expected generated path:

```text
/Users/blackie/Projects/raver/mobile/flutter_raver_android/app/raver_android
```

Use:

```bash
cd /Users/blackie/Projects/raver/mobile/flutter_raver_android
./scripts/bootstrap_flutter_project.sh
```

This placeholder exists because Flutter is not installed on this machine yet, so the real project was not generated by this pass.

