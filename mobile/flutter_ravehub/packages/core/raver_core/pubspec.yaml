name: raver_core
description: Core configuration, constants, errors, and extensions for RaveHub
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.5.0

dependencies:
  meta: ^1.12.0

dev_dependencies:
  very_good_analysis: ^6.0.0
  test: ^1.25.0
